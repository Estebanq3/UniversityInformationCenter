CREATE TABLE [dbo].[UniversityCollaborations]
(
	[Id] INT NOT NULL, 
    [CollaboratorUniversity] INT NOT NULL, 
    [UniversityGroup] INT NOT NULL, 
    [CollaboratorGroupUniversity] INT NOT NULL 
)
